# How to perform the inpainting
To perform the inpainting, run `main_inpainting.m`.

This will save the inpainted volumes in the folder `Inpainting_results` inside 
'data/ the dataset with which you work'